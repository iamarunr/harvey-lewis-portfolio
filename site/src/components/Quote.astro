---
---

<figure class="quote" data-reveal>
  <blockquote>
    “We are all capable of incredible feats. No matter your pursuits, <span
      >we can all go so much further than we think.</span
    >”
  </blockquote>
  <figcaption>Rich Roll Podcast · Episode 812</figcaption>
</figure>

<style>
  .quote {
    margin: clamp(60px, 7vw, 100px) auto 0;
    max-width: 1080px;
  }

  blockquote {
    font-family: var(--font-serif);
    text-align: center;
    font-weight: 500;
    font-size: clamp(26px, 3.2vw, 48px);
    line-height: 1.3;
    letter-spacing: -0.015em;
    text-wrap: pretty;
    hanging-punctuation: first last;
  }

  blockquote span {
    color: var(--rust);
  }

  figcaption {
    text-align: center;
    font-family: var(--font-mono);
    font-size: clamp(12px, 0.85vw, 13px);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--ink-dim);
    margin-top: 24px;
  }
</style>
